
# Thiqa — Empty Project Setup

Set up a clean, empty starting point for the Thiqa project:

1. **Clean the Index page** — Remove the placeholder content and replace with a minimal welcome screen showing "Thiqa" as the project name with a clean, centered layout.

2. **No database or external connections** — Keep the project as a pure frontend React app with no Supabase, no API integrations, and no external services.
